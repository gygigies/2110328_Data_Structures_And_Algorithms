#include <bits/stdc++.h>
#include <list>
using namespace std;
int main() {
    int n , k , v ;
    cin >> n >> k >> v;
    int color;
    list <int> input;
    while(n--) {
        cin >> color ;
        input.pusback(color);
    }
    

}